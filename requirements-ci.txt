# requirements-ci.txt
# Lightweight CI-only dependencies — NO PyTorch, NO heavy ML libs
# Heavy deps (sentence-transformers, qdrant-client) are mocked in conftest.py
# Target: Install stage < 30 seconds

fastapi
uvicorn
pytest
httpx
python-json-logger
loguru
pydantic-settings
python-multipart
anyio[asyncio]
tqdm
numpy
python-dotenv