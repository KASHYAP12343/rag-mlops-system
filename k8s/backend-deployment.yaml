apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-backend
  namespace: rag-system
  labels:
    app: rag-api
    version: "1.0"
    project: rag-mlops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rag-api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1          # allow 1 extra pod during update
      maxUnavailable: 0    # zero downtime — never kill before new pod is ready
  template:
    metadata:
      labels:
        app: rag-api
        version: "1.0"
    spec:
      containers:
        - name: rag-api
          image: mayank2101/rag-mlops:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http
          # ── Resource limits ───────────────────────────────────────────────
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "2Gi"
          # ── Environment from ConfigMap ────────────────────────────────────
          envFrom:
            - configMapRef:
                name: rag-config
          # ── Sensitive env from Secret ─────────────────────────────────────
          env:
            - name: GROQ_API_KEY
              valueFrom:
                secretKeyRef:
                  name: rag-secrets
                  key: GROQ_API_KEY
          # ── Liveness probe — restart if API becomes unresponsive ──────────
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 120   # was 40 — model loads in ~30s, give headroom
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 5        # was 3 — more tolerance before restart
          # ── Readiness probe — stop traffic if dependencies are down ───────
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 120   # was 45 — wait for model load + Qdrant connect
            periodSeconds: 20          # was 15 — slightly less aggressive
            timeoutSeconds: 10
            failureThreshold: 6        # was 3 — 6×20s = 2 min grace after initial delay
          # ── Log volume ────────────────────────────────────────────────────
          volumeMounts:
            - name: log-volume
              mountPath: /app/logs
      volumes:
        - name: log-volume
          emptyDir: {}