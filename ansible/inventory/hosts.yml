---
# ansible/inventory/hosts.yml
# Single-machine demo: deploy to localhost using local connection
# (no SSH needed — Ansible runs commands directly on this machine)

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"

  children:
    rag_servers:
      hosts:
        localhost:
